# SwiftPad
SwiftPad is a plain-text notepad for macOS with one trick most editors don't bother with: pick a theme and the **whole window** recolors — tab bar, menu bar, status bar, and the page itself — the way Terminal.app themes a window, not just the text you're typing.
